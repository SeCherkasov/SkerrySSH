package app.skerry.shared.sync

/**
 * Клиентская часть self-hosted sync (Phase 2). Протокол, LWW-конфликты и паринг —
 * `docs/skerry-sync-design.md`. До Phase 2 модуль остаётся интерфейсом.
 */
interface SyncEngine
