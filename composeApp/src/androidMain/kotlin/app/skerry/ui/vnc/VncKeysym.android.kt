package app.skerry.ui.vnc

import androidx.compose.ui.input.key.Key

// Android keycodes stop at F12, so there is nothing to add here.
internal actual val platformKeySymExtras: Map<Key, Long> = emptyMap()
